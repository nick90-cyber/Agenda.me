notion-calendar/
│── index.html          ← Página principal (HTML base)
│── package.json        ← Configuração do Node.js e dependências
│── vite.config.js      ← Configuração do Vite
│── tailwind.config.js  ← Configuração do Tailwind
│── postcss.config.js   ← Configuração de estilos
│
└── src/                ← 📌 Aqui fica o código do seu app React
    ├── main.jsx        ← Arquivo que inicializa a aplicação React
    ├── index.css       ← Estilos globais + Tailwind
    └── App.jsx         ← 📌 Aqui está o calendário e todas as funcionalidades
